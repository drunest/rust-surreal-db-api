# REST API made with Rust
This is a RUST API made with `Rust`


* Make sure your surreal db server is running and add correct username and password to connect to the database if authentication is enabled